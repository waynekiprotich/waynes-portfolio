# Wayne Kiprotich — portfolio (frontend only)
#
# This rebuild has no backend. Nothing here is required to run the site;
# the file exists so a future variable has an obvious home.
#
# Anything prefixed VITE_ is public once built — never put a secret here.
